<% if @journal.private_notes? %>
  (<%= l(:field_private_notes) %>)
<% end %>
<%= l(:text_issue_updated, :id => "##{@issue.id}", :author => h(@changer)) %>

<hr />
<pre><%= JournalChangeDiffer.new(@journal).diff %></pre>
<hr />
<%= render :partial => 'mailer/issue',
           :formats => [:html],
           :locals => {
             :issue => @issue,
             :user => @user, # For Redmine 4 or later
             :users => @users, # For Redmine 3 or ealier
             :issue_url => @issue_url
           } %>
