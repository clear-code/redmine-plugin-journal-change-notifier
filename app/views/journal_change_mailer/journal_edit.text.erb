<%= "(#{l(:field_private_notes)}) " if @journal.private_notes? -%><%= l(:text_issue_updated, :id => "##{@issue.id}", :author => @changer) %>

============================================================
<%= JournalChangeDiffer.new(@journal).diff.html_safe %>
============================================================

----------------------------------------
<%= render :partial => 'mailer/issue',
           :formats => [:text],
           :locals => {
              :issue => @issue,
              :user => @user, # For Redmine 4 or later
              :users => @users, # For Redmine 3 or ealier
              :issue_url => @issue_url
           } %>
